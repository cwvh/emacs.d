;; Configuration that is unique to various systems I
;; routinely use Emacs on. Below is just an example for
;; the git tree; I will override fonts and sizes depending
;; on what is available on the host system (or renders best.)

(when window-system (set-frame-size (selected-frame) 100 50))
